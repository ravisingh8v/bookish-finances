diff --git a/src/hooks/useBooks.ts b/src/hooks/useBooks.ts
index 854e9f1..9891e34 100644
--- a/src/hooks/useBooks.ts
+++ b/src/hooks/useBooks.ts
@@ -273,7 +273,8 @@ export function useBooks() {
           book_id: newBook.id,
           title: expense.title,
           amount: expense.amount,
-          date: expense.date,
+          date: expense.expense_date ?? expense.date,
+          expense_date: expense.expense_date ?? expense.date,
           category_id: expense.category_id || null,
           expense_type: expense.expense_type ?? "debit",
           payment_method: expense.payment_method ?? "cash",
diff --git a/src/hooks/useExpenses.ts b/src/hooks/useExpenses.ts
index e43eafb..46ece4c 100644
--- a/src/hooks/useExpenses.ts
+++ b/src/hooks/useExpenses.ts
@@ -23,6 +23,7 @@ export interface Expense {
   title: string;
   amount: number;
   date: string;
+  expense_date: string;
   category_id: string | null;
   expense_type: string;
   payment_method: string | null;
@@ -47,6 +48,7 @@ type ExpensePayload = {
   book_id: string;
   title: string;
   amount: number;
+  expense_date?: string;
   date?: string;
   category_id?: string;
   category?: Pick<Category, "name" | "icon" | "color"> | null;
@@ -60,6 +62,7 @@ type ExpenseUpdate = {
   expenseId: string;
   title?: string;
   amount?: number;
+  expense_date?: string;
   date?: string;
   category_id?: string;
   category?: Pick<Category, "name" | "icon" | "color"> | null;
@@ -80,7 +83,9 @@ function getTimestamp(value?: string | null) {
 
 function sortExpensesByDateDesc(expenses: Expense[]) {
   return [...expenses].sort((a, b) => {
-    const dateDiff = getTimestamp(b.date) - getTimestamp(a.date);
+    const dateDiff =
+      getTimestamp(b.expense_date ?? b.date) -
+      getTimestamp(a.expense_date ?? a.date);
     if (dateDiff !== 0) return dateDiff;
     const createdDiff = getTimestamp(b.created_at) - getTimestamp(a.created_at);
     if (createdDiff !== 0) return createdDiff;
@@ -142,9 +147,11 @@ function getCustomCategoryColor(name: string) {
 async function fetchExpensesPage(bookId: string, from: number, to: number) {
   const { data, error } = await supabase
     .from("expenses")
-    .select("*, categories(name, icon, color)")
+    .select(
+      "id, book_id, title, amount, date, expense_date, category_id, expense_type, payment_method, notes, tags, paid_by, created_by, created_at, updated_at, categories(name, icon, color)",
+    )
     .eq("book_id", bookId)
-    .order("date", { ascending: false })
+    .order("expense_date", { ascending: false })
     .order("created_at", { ascending: false })
     .range(from, to);
   if (error) throw error;
@@ -169,6 +176,8 @@ async function fetchExpensesPage(bookId: string, from: number, to: number) {
 
   return (data ?? []).map((expense) => ({
     ...expense,
+    date: expense.date ?? expense.expense_date,
+    expense_date: expense.expense_date ?? expense.date,
     creator_profile: profileMap.get(expense.created_by) ?? null,
     payer_profile: profileMap.get(expense.paid_by) ?? null,
   })) as Expense[];
@@ -265,13 +274,16 @@ export function useExpenses(bookId: string) {
       const uid = user?.id;
       if (!uid) throw new Error("User ID not available. Please log in again.");
 
+      const expenseDate =
+        payload.expense_date ?? payload.date ?? new Date().toISOString();
       const { data, error } = await supabase
         .from("expenses")
         .insert({
           book_id: payload.book_id,
           title: payload.title,
           amount: payload.amount,
-          date: payload.date ?? new Date().toISOString(),
+          date: expenseDate,
+          expense_date: expenseDate,
           category_id: payload.category_id || null,
           expense_type: payload.expense_type ?? "debit",
           payment_method: payload.payment_method ?? "cash",
@@ -294,6 +306,10 @@ export function useExpenses(bookId: string) {
       const update: TablesUpdate<"expenses"> = {};
       if (params.title !== undefined) update.title = params.title;
       if (params.amount !== undefined) update.amount = params.amount;
+      if (params.expense_date !== undefined) {
+        update.expense_date = params.expense_date;
+        update.date = params.expense_date;
+      }
       if (params.date !== undefined) update.date = params.date;
       if (params.category_id !== undefined)
         update.category_id = params.category_id || null;
diff --git a/src/integrations/supabase/types.ts b/src/integrations/supabase/types.ts
index d9e9b8d..71d2b8d 100644
--- a/src/integrations/supabase/types.ts
+++ b/src/integrations/supabase/types.ts
@@ -134,6 +134,7 @@ export type Database = {
           created_at: string
           created_by: string
           date: string
+          expense_date: string
           expense_type: string
           id: string
           notes: string | null
@@ -150,6 +151,7 @@ export type Database = {
           created_at?: string
           created_by: string
           date?: string
+          expense_date?: string
           expense_type?: string
           id?: string
           notes?: string | null
@@ -166,6 +168,7 @@ export type Database = {
           created_at?: string
           created_by?: string
           date?: string
+          expense_date?: string
           expense_type?: string
           id?: string
           notes?: string | null
diff --git a/src/pages/Analytics.tsx b/src/pages/Analytics.tsx
index 89331ed..2ac6493 100644
--- a/src/pages/Analytics.tsx
+++ b/src/pages/Analytics.tsx
@@ -87,7 +87,7 @@ export default function Analytics() {
         if (e.expense_type === "debit")
           catMap[catName].total += Number(e.amount);
 
-        const month = new Date(e.date).toLocaleDateString("en", {
+        const month = new Date(e.expense_date ?? e.date).toLocaleDateString("en", {
           month: "short",
           year: "2-digit",
         });
diff --git a/src/pages/BookDetail.tsx b/src/pages/BookDetail.tsx
index 14631d5..29fcb1f 100644
--- a/src/pages/BookDetail.tsx
+++ b/src/pages/BookDetail.tsx
@@ -116,37 +116,67 @@ function getRequiredCategoryId(
   );
 }
 
-function getExpenseDateKey(expense: { date?: string | null }) {
-  return expense.date?.split("T")[0] ?? "";
+function normalizeDateTimeValue(value?: string | null) {
+  if (!value) return value;
+  return value.replace(" ", "T");
+}
+
+function getExpenseDateKey(expense: { date?: string | null; expense_date?: string | null }) {
+  return normalizeDateTimeValue(expense.expense_date ?? expense.date)?.split("T")[0] ?? "";
+}
+
+function pad2(value: number) {
+  return value.toString().padStart(2, "0");
 }
 
 function toDateTimeLocalValue(value?: string | null) {
-  const source = value ? new Date(value) : new Date();
+  const source = value ? new Date(normalizeDateTimeValue(value)) : new Date();
   const date = Number.isNaN(source.getTime()) ? new Date() : source;
-  const offsetDate = new Date(date.getTime() - date.getTimezoneOffset() * 60000);
-  return offsetDate.toISOString().slice(0, 16);
+  return `${date.getFullYear()}-${pad2(date.getMonth() + 1)}-${pad2(
+    date.getDate(),
+  )}T${pad2(date.getHours())}:${pad2(date.getMinutes())}`;
+}
+
+function parseLocalDateTime(value: string) {
+  const [datePart, timePart] = value.split("T");
+  if (!datePart || !timePart) return null;
+
+  const [year, month, day] = datePart.split("-").map(Number);
+  const [hour, minute] = timePart.split(":").map(Number);
+  if (
+    [year, month, day, hour, minute].some(
+      (component) => !Number.isFinite(component),
+    )
+  ) {
+    return null;
+  }
+
+  return new Date(year, month - 1, day, hour, minute);
 }
 
 function toUtcDateTime(value: string) {
-  const date = new Date(value);
-  return Number.isNaN(date.getTime()) ? new Date().toISOString() : date.toISOString();
+  const date = parseLocalDateTime(value);
+  return date ? date.toISOString() : new Date().toISOString();
 }
 
 function getTimestamp(value?: string | null) {
   if (!value) return 0;
-  const time = new Date(value).getTime();
+  const time = new Date(normalizeDateTimeValue(value)).getTime();
   return Number.isNaN(time) ? 0 : time;
 }
 
 function sortExpensesByDateDesc<
   T extends {
     date?: string | null;
+    expense_date?: string | null;
     created_at?: string | null;
     id: string;
   },
 >(expenses: T[]) {
   return [...expenses].sort((a, b) => {
-    const dateDiff = getTimestamp(b.date) - getTimestamp(a.date);
+    const dateDiff =
+      getTimestamp(b.expense_date ?? b.date) -
+      getTimestamp(a.expense_date ?? a.date);
     if (dateDiff !== 0) return dateDiff;
 
     const createdDiff = getTimestamp(b.created_at) - getTimestamp(a.created_at);
@@ -156,16 +186,20 @@ function sortExpensesByDateDesc<
   });
 }
 
-function formatExpenseDate(date?: string | null) {
-  if (date?.includes("T")) {
-    return new Date(date).toLocaleDateString("en-IN", {
+function formatExpenseDate(expense?: { date?: string | null; expense_date?: string | null }) {
+  const normalized = normalizeDateTimeValue(expense?.expense_date ?? expense?.date);
+  if (normalized) {
+    return new Date(normalized).toLocaleDateString("en-IN", {
       month: "short",
       day: "numeric",
       year: "2-digit",
     });
   }
 
-  const [year, month, day] = getExpenseDateKey({ date }).split("-").map(Number);
+  const [year, month, day] = getExpenseDateKey({
+    date: expense?.date,
+    expense_date: expense?.expense_date,
+  }).split("-").map(Number);
   if (!year || !month || !day) return "";
 
   return new Date(year, month - 1, day).toLocaleDateString("en-IN", {
@@ -177,17 +211,16 @@ function formatExpenseDate(date?: string | null) {
 
 function formatExpenseActivityTime(expense: {
   date?: string | null;
+  expense_date?: string | null;
   created_at?: string | null;
   updated_at?: string | null;
 }) {
-  // Prefer the user-chosen expense date/time when it carries a time component,
-  // falling back to the activity timestamps otherwise.
+  const normalizedDate = normalizeDateTimeValue(expense.expense_date ?? expense.date);
   const timestamp =
-    expense.date?.includes("T")
-      ? expense.date
-      : expense.updated_at && expense.updated_at !== expense.created_at
-        ? expense.updated_at
-        : expense.created_at;
+    normalizedDate ||
+    (expense.updated_at && expense.updated_at !== expense.created_at
+      ? normalizeDateTimeValue(expense.updated_at)
+      : normalizeDateTimeValue(expense.created_at));
   if (!timestamp) return "";
 
   return new Date(timestamp).toLocaleTimeString("en-IN", {
@@ -401,7 +434,7 @@ export default function BookDetail() {
   const handleEditExpense = (expense: (typeof expenses)[number]) => {
     setTitle(expense.title);
     setAmount(String(expense.amount));
-    setDate(toDateTimeLocalValue(expense.date));
+    setDate(toDateTimeLocalValue(expense.expense_date ?? expense.date));
     setCategoryId(getRequiredCategoryId(categories, expense.category_id));
     setExpenseType(expense.expense_type ?? "debit");
     setPaymentMethod(expense.payment_method ?? "cash");
@@ -431,6 +464,7 @@ export default function BookDetail() {
       title: title.trim(),
       amount: Number(amount),
       date: toUtcDateTime(date),
+      expense_date: toUtcDateTime(date),
       category_id: selectedCategory.id,
       category: {
         name: selectedCategory.name,
@@ -1116,7 +1150,7 @@ export default function BookDetail() {
 
                           {/* Bottom Row: Expense Date + Activity Time */}
                           <div className="text-[9px] sm:text-[10px] text-muted-foreground/50 pt-1.5 border-t border-border/50 pl-0 sm:pl-10">
-                            {formatExpenseDate(expense.date)}
+                            {formatExpenseDate(expense)}
                             {formatExpenseActivityTime(expense)
                               ? ` • ${formatExpenseActivityTime(expense)}`
                               : ""}
